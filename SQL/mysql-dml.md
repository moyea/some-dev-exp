
# MySQL DML相关

```sql
show databases; -- 查看数据库

use databese_name; -- 指定数据库

show tables;   -- 查看指定数据库(use database_name)中的表

show cloumns from table_name; -- 查看table_name的表结构

(desc|describe) table_name;  -- 查看表结构

show create table table_name; -- 查看建表语句
```
